namespace SecComServer
{
    public static class ServiceExtensions
    {
        //public static void AddElasticSearch(this IServiceCollection collection)
        //{
        //    collection.AddTransient(sp =>
        //    {
        //        var connectionSettings = new ConnectionSettings(new Uri("http://localhost:9200"))
        //            .DefaultIndex("SecCom");
        //        return connectionSettings;
        //    });
        //    collection.AddSingleton(sp => new ElasticClient(sp.GetService<ConnectionSettings>()));
        //}

    }
}
